# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Tests / coverage
coverage/
.nyc_output/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Env / secrets
.env
.env.*
!.env.example

# Local scratch / temp
tmp/
.tmp/
*.tmp

# Bundled distribution archives (Ralph hand-off)
rhwp-mcp-server-*.tar.gz
rhwp-mcp-server-*.zip

# OMC planning artifacts (deep-interview spec, omc-plan, ralph PRD, etc.)
# These belong to the maintainer's local workspace, not the published repo.
# Curated copies of the spec / plan can live under docs/planning/ if desired.
.omc/

# Session-specific transient files
voice_*.ogg
warm.log

# Auto-generated runtime probe outputs (regenerated by `npm run probe:fields`
# and `npm run measure:tokens` — useful to commit a snapshot, but not the
# rebuild churn). Comment these out to commit the latest probe baseline.
# docs/measurements/tool-description-tokens.txt
# docs/measurements/rhwp-field-api.md
